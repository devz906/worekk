# Build GameHub. With secrets → IPA for device. Without secrets → build for simulator only (no failure).

name: Build IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Check if signing secrets are set
        id: secrets
        run: |
          if [ -n "${{ secrets.DEVELOPER_TEAM_ID }}" ] && [ -n "${{ secrets.CODE_SIGNING_IDENTITY }}" ] && [ -n "${{ secrets.P12_BASE64 }}" ] && [ -n "${{ secrets.P12_PASSWORD }}" ] && [ -n "${{ secrets.MOBILEPROVISION_BASE64 }}" ]; then
            echo "have_secrets=true" >> $GITHUB_OUTPUT
          else
            echo "have_secrets=false" >> $GITHUB_OUTPUT
            echo "::notice::No code signing secrets. Building for simulator only. To get an IPA, add secrets (see BUILD_IPA.md)."
          fi

      - name: Build and export IPA
        if: steps.secrets.outputs.have_secrets == 'true'
        uses: yukiarrr/ios-build-action@v1.12.0
        with:
          project-path: GameHub.xcodeproj
          scheme: GameHub
          configuration: Release
          export-method: development
          team-id: ${{ secrets.DEVELOPER_TEAM_ID }}
          code-signing-identity: ${{ secrets.CODE_SIGNING_IDENTITY }}
          p12-base64: ${{ secrets.P12_BASE64 }}
          certificate-password: ${{ secrets.P12_PASSWORD }}
          mobileprovision-base64: ${{ secrets.MOBILEPROVISION_BASE64 }}
          output-path: GameHub.ipa

      - name: Upload IPA
        if: steps.secrets.outputs.have_secrets == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: GameHub-iPA
          path: GameHub.ipa

      - name: Build for simulator (no signing)
        if: steps.secrets.outputs.have_secrets == 'false'
        run: |
          xcodebuild -project GameHub.xcodeproj -scheme GameHub -configuration Release \
            -destination 'generic/platform=iOS Simulator' -derivedDataPath build \
            build
          echo "Build OK. Add the 5 secrets in repo Settings → Secrets and variables → Actions to get an IPA. See BUILD_IPA.md."
